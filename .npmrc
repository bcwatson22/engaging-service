# Pin exactly on `pnpm add`, so a version is only ever raised deliberately —
# by the monthly dependency routine or by hand — never by a range resolving
# to something newer. Without this, pnpm writes ^ by default.
save-exact=true
